<?xml version="1.0" encoding="UTF-8"?>
<!-- The above is a default, but its inclusion should be everywhere just to indicate that all files, for our purposes, are UTF-8 encoded.  Also, as a reminder, the less than, greater than, ampersand, apostrophe, and quotation mark are invalid XML and must be replaced with the literal text &lt; &gt; &amp; &apos; &quot; with the semicolons -->
<core_data>
  <spritesheet name="TextButton_Common">
    <mSheetTypeName>Button</mSheetTypeName>
	<mPath>_Previs Menus_and_Buttons/Buttons/Common/textbutton_common</mPath>
	<mSpriteList>
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>0</id> <!-- integer, ids must be unique and flush -->
		<name>body_unpressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>0</y_origin> <!-- the top of an image is 0 -->
		<width>49</width>
		<height>50</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>1</id> <!-- integer, ids must be unique and flush -->
		<name>body_corner_unpressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>51</y_origin> <!-- the top of an image is 0 -->
		<width>6</width>
		<height>6</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>2</id> <!-- integer, ids must be unique and flush -->
		<name>body_side_unpressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>58</y_origin> <!-- the top of an image is 0 -->
		<width>6</width>
		<height>1</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>3</id> <!-- integer, ids must be unique and flush -->
		<name>body_pressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>60</y_origin> <!-- the top of an image is 0 -->
		<width>49</width>
		<height>50</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>4</id> <!-- integer, ids must be unique and flush -->
		<name>body_corner_pressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>111</y_origin> <!-- the top of an image is 0 -->
		<width>6</width>
		<height>6</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>5</id> <!-- integer, ids must be unique and flush -->
		<name>body_side_pressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>118</y_origin> <!-- the top of an image is 0 -->
		<width>6</width>
		<height>1</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	</mSpriteList>
    <mTagList></mTagList>
  </spritesheet>
  
  <spritesheet name="ToggleButtonOn_Common">
    <mSheetTypeName>Button</mSheetTypeName>
	<mPath>_Previs Menus_and_Buttons/Buttons/Common/togglebuttonon_common</mPath>
	<mSpriteList>
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>0</id> <!-- integer, ids must be unique and flush -->
		<name>body_unpressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>0</y_origin> <!-- the top of an image is 0 -->
		<width>40</width>
		<height>40</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>1</id> <!-- integer, ids must be unique and flush -->
		<name>body_corner_unpressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>41</y_origin> <!-- the top of an image is 0 -->
		<width>1</width>
		<height>1</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>2</id> <!-- integer, ids must be unique and flush -->
		<name>body_side_unpressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>43</y_origin> <!-- the top of an image is 0 -->
		<width>1</width>
		<height>1</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>3</id> <!-- integer, ids must be unique and flush -->
		<name>body_pressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>45</y_origin> <!-- the top of an image is 0 -->
		<width>40</width>
		<height>40</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>4</id> <!-- integer, ids must be unique and flush -->
		<name>body_corner_pressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>86</y_origin> <!-- the top of an image is 0 -->
		<width>1</width>
		<height>1</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>5</id> <!-- integer, ids must be unique and flush -->
		<name>body_side_pressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>88</y_origin> <!-- the top of an image is 0 -->
		<width>1</width>
		<height>1</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	</mSpriteList>
    <mTagList></mTagList>
  </spritesheet>
  
  <spritesheet name="ToggleButtonOff_Common">
    <mSheetTypeName>Button</mSheetTypeName>
	<mPath>_Previs Menus_and_Buttons/Buttons/Common/togglebuttonoff_common</mPath>
	<mSpriteList>
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>0</id> <!-- integer, ids must be unique and flush -->
		<name>body_unpressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>0</y_origin> <!-- the top of an image is 0 -->
		<width>40</width>
		<height>40</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>1</id> <!-- integer, ids must be unique and flush -->
		<name>body_corner_unpressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>41</y_origin> <!-- the top of an image is 0 -->
		<width>1</width>
		<height>1</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>2</id> <!-- integer, ids must be unique and flush -->
		<name>body_side_unpressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>43</y_origin> <!-- the top of an image is 0 -->
		<width>1</width>
		<height>1</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>3</id> <!-- integer, ids must be unique and flush -->
		<name>body_pressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>45</y_origin> <!-- the top of an image is 0 -->
		<width>40</width>
		<height>40</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>4</id> <!-- integer, ids must be unique and flush -->
		<name>body_corner_pressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>86</y_origin> <!-- the top of an image is 0 -->
		<width>1</width>
		<height>1</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>5</id> <!-- integer, ids must be unique and flush -->
		<name>body_side_pressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>88</y_origin> <!-- the top of an image is 0 -->
		<width>1</width>
		<height>1</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	</mSpriteList>
    <mTagList></mTagList>
  </spritesheet>
  
  <spritesheet name="DownButton_Common">
    <mSheetTypeName>Button</mSheetTypeName>
	<mPath>_Previs Menus_and_Buttons/Buttons/Common/downbutton_common</mPath>
	<mSpriteList>
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>0</id> <!-- integer, ids must be unique and flush -->
		<name>body_unpressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>0</y_origin> <!-- the top of an image is 0 -->
		<width>40</width>
		<height>40</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>1</id> <!-- integer, ids must be unique and flush -->
		<name>body_corner_unpressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>41</y_origin> <!-- the top of an image is 0 -->
		<width>1</width>
		<height>1</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>2</id> <!-- integer, ids must be unique and flush -->
		<name>body_side_unpressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>43</y_origin> <!-- the top of an image is 0 -->
		<width>1</width>
		<height>1</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>3</id> <!-- integer, ids must be unique and flush -->
		<name>body_pressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>45</y_origin> <!-- the top of an image is 0 -->
		<width>40</width>
		<height>40</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>4</id> <!-- integer, ids must be unique and flush -->
		<name>body_corner_pressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>86</y_origin> <!-- the top of an image is 0 -->
		<width>1</width>
		<height>1</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>5</id> <!-- integer, ids must be unique and flush -->
		<name>body_side_pressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>88</y_origin> <!-- the top of an image is 0 -->
		<width>1</width>
		<height>1</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	</mSpriteList>
    <mTagList></mTagList>
  </spritesheet>
  
  <spritesheet name="UpButton_Common">
    <mSheetTypeName>Button</mSheetTypeName>
	<mPath>_Previs Menus_and_Buttons/Buttons/Common/upbutton_common</mPath>
	<mSpriteList>
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>0</id> <!-- integer, ids must be unique and flush -->
		<name>body_unpressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>0</y_origin> <!-- the top of an image is 0 -->
		<width>40</width>
		<height>40</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>1</id> <!-- integer, ids must be unique and flush -->
		<name>body_corner_unpressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>41</y_origin> <!-- the top of an image is 0 -->
		<width>1</width>
		<height>1</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>2</id> <!-- integer, ids must be unique and flush -->
		<name>body_side_unpressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>43</y_origin> <!-- the top of an image is 0 -->
		<width>1</width>
		<height>1</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>3</id> <!-- integer, ids must be unique and flush -->
		<name>body_pressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>45</y_origin> <!-- the top of an image is 0 -->
		<width>40</width>
		<height>40</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>4</id> <!-- integer, ids must be unique and flush -->
		<name>body_corner_pressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>86</y_origin> <!-- the top of an image is 0 -->
		<width>1</width>
		<height>1</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>5</id> <!-- integer, ids must be unique and flush -->
		<name>body_side_pressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>88</y_origin> <!-- the top of an image is 0 -->
		<width>1</width>
		<height>1</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	</mSpriteList>
    <mTagList></mTagList>
  </spritesheet>
  
  <spritesheet name="LeftButton_Common">
    <mSheetTypeName>Button</mSheetTypeName>
	<mPath>_Previs Menus_and_Buttons/Buttons/Common/leftbutton_common</mPath>
	<mSpriteList>
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>0</id> <!-- integer, ids must be unique and flush -->
		<name>body_unpressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>0</y_origin> <!-- the top of an image is 0 -->
		<width>40</width>
		<height>40</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>1</id> <!-- integer, ids must be unique and flush -->
		<name>body_corner_unpressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>41</y_origin> <!-- the top of an image is 0 -->
		<width>1</width>
		<height>1</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>2</id> <!-- integer, ids must be unique and flush -->
		<name>body_side_unpressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>43</y_origin> <!-- the top of an image is 0 -->
		<width>1</width>
		<height>1</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>3</id> <!-- integer, ids must be unique and flush -->
		<name>body_pressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>45</y_origin> <!-- the top of an image is 0 -->
		<width>40</width>
		<height>40</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>4</id> <!-- integer, ids must be unique and flush -->
		<name>body_corner_pressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>86</y_origin> <!-- the top of an image is 0 -->
		<width>1</width>
		<height>1</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>5</id> <!-- integer, ids must be unique and flush -->
		<name>body_side_pressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>88</y_origin> <!-- the top of an image is 0 -->
		<width>1</width>
		<height>1</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	</mSpriteList>
    <mTagList></mTagList>
  </spritesheet>
  
  <spritesheet name="RightButton_Common">
    <mSheetTypeName>Button</mSheetTypeName>
	<mPath>_Previs Menus_and_Buttons/Buttons/Common/rightbutton_common</mPath>
	<mSpriteList>
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>0</id> <!-- integer, ids must be unique and flush -->
		<name>body_unpressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>0</y_origin> <!-- the top of an image is 0 -->
		<width>40</width>
		<height>40</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>1</id> <!-- integer, ids must be unique and flush -->
		<name>body_corner_unpressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>41</y_origin> <!-- the top of an image is 0 -->
		<width>1</width>
		<height>1</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>2</id> <!-- integer, ids must be unique and flush -->
		<name>body_side_unpressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>43</y_origin> <!-- the top of an image is 0 -->
		<width>1</width>
		<height>1</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>3</id> <!-- integer, ids must be unique and flush -->
		<name>body_pressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>45</y_origin> <!-- the top of an image is 0 -->
		<width>40</width>
		<height>40</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>4</id> <!-- integer, ids must be unique and flush -->
		<name>body_corner_pressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>86</y_origin> <!-- the top of an image is 0 -->
		<width>1</width>
		<height>1</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>5</id> <!-- integer, ids must be unique and flush -->
		<name>body_side_pressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>88</y_origin> <!-- the top of an image is 0 -->
		<width>1</width>
		<height>1</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	</mSpriteList>
    <mTagList></mTagList>
  </spritesheet>
  
  <spritesheet name="slowdown_button">
    <mSheetTypeName>Button</mSheetTypeName>
	<mPath>_Previs Menus_and_Buttons/Buttons/Common/slowdown_button</mPath>
	<mSpriteList>
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>0</id> <!-- integer, ids must be unique and flush -->
		<name>body_unpressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>0</y_origin> <!-- the top of an image is 0 -->
		<width>40</width>
		<height>39</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>1</id> <!-- integer, ids must be unique and flush -->
		<name>body_corner_unpressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>0</y_origin> <!-- the top of an image is 0 -->
		<width>0</width>
		<height>0</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>2</id> <!-- integer, ids must be unique and flush -->
		<name>body_side_unpressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>0</y_origin> <!-- the top of an image is 0 -->
		<width>0</width>
		<height>0</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>3</id> <!-- integer, ids must be unique and flush -->
		<name>body_pressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>40</y_origin> <!-- the top of an image is 0 -->
		<width>40</width>
		<height>39</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>4</id> <!-- integer, ids must be unique and flush -->
		<name>body_corner_pressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>0</y_origin> <!-- the top of an image is 0 -->
		<width>0</width>
		<height>0</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>5</id> <!-- integer, ids must be unique and flush -->
		<name>body_side_pressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>0</y_origin> <!-- the top of an image is 0 -->
		<width>0</width>
		<height>0</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	</mSpriteList>
    <mTagList></mTagList>
  </spritesheet>
  
  <spritesheet name="speedup_button">
    <mSheetTypeName>Button</mSheetTypeName>
	<mPath>_Previs Menus_and_Buttons/Buttons/Common/speedupbutton</mPath>
	<mSpriteList>
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>0</id> <!-- integer, ids must be unique and flush -->
		<name>body_unpressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>0</y_origin> <!-- the top of an image is 0 -->
		<width>40</width>
		<height>39</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>1</id> <!-- integer, ids must be unique and flush -->
		<name>body_corner_unpressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>0</y_origin> <!-- the top of an image is 0 -->
		<width>0</width>
		<height>0</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>2</id> <!-- integer, ids must be unique and flush -->
		<name>body_side_unpressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>0</y_origin> <!-- the top of an image is 0 -->
		<width>0</width>
		<height>0</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>3</id> <!-- integer, ids must be unique and flush -->
		<name>body_pressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>40</y_origin> <!-- the top of an image is 0 -->
		<width>40</width>
		<height>39</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>4</id> <!-- integer, ids must be unique and flush -->
		<name>body_corner_pressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>0</y_origin> <!-- the top of an image is 0 -->
		<width>0</width>
		<height>0</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>5</id> <!-- integer, ids must be unique and flush -->
		<name>body_side_pressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>0</y_origin> <!-- the top of an image is 0 -->
		<width>0</width>
		<height>0</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	</mSpriteList>
    <mTagList></mTagList>
  </spritesheet>
  
  <spritesheet name="smoldown_button">
    <mSheetTypeName>Button</mSheetTypeName>
	<mPath>_Previs Menus_and_Buttons/Buttons/smol button down</mPath>
	<mSpriteList>
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>0</id> <!-- integer, ids must be unique and flush -->
		<name>body_unpressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>0</y_origin> <!-- the top of an image is 0 -->
		<width>21</width>
		<height>10</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>1</id> <!-- integer, ids must be unique and flush -->
		<name>body_corner_unpressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>0</y_origin> <!-- the top of an image is 0 -->
		<width>0</width>
		<height>0</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>2</id> <!-- integer, ids must be unique and flush -->
		<name>body_side_unpressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>0</y_origin> <!-- the top of an image is 0 -->
		<width>0</width>
		<height>0</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>3</id> <!-- integer, ids must be unique and flush -->
		<name>body_pressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>11</y_origin> <!-- the top of an image is 0 -->
		<width>21</width>
		<height>10</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>4</id> <!-- integer, ids must be unique and flush -->
		<name>body_corner_pressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>0</y_origin> <!-- the top of an image is 0 -->
		<width>0</width>
		<height>0</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>5</id> <!-- integer, ids must be unique and flush -->
		<name>body_side_pressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>0</y_origin> <!-- the top of an image is 0 -->
		<width>0</width>
		<height>0</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	</mSpriteList>
    <mTagList></mTagList>
  </spritesheet>
  
  <spritesheet name="smolup_button">
    <mSheetTypeName>Button</mSheetTypeName>
	<mPath>_Previs Menus_and_Buttons/Buttons/smol button up</mPath>
	<mSpriteList>
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>0</id> <!-- integer, ids must be unique and flush -->
		<name>body_unpressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>0</y_origin> <!-- the top of an image is 0 -->
		<width>21</width>
		<height>10</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>1</id> <!-- integer, ids must be unique and flush -->
		<name>body_corner_unpressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>0</y_origin> <!-- the top of an image is 0 -->
		<width>0</width>
		<height>0</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>2</id> <!-- integer, ids must be unique and flush -->
		<name>body_side_unpressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>0</y_origin> <!-- the top of an image is 0 -->
		<width>0</width>
		<height>0</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>3</id> <!-- integer, ids must be unique and flush -->
		<name>body_pressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>11</y_origin> <!-- the top of an image is 0 -->
		<width>21</width>
		<height>10</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>4</id> <!-- integer, ids must be unique and flush -->
		<name>body_corner_pressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>0</y_origin> <!-- the top of an image is 0 -->
		<width>0</width>
		<height>0</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	  
	  <sprite> <!-- name here does not actually matter, neither does order of sprites -->
	    <id>5</id> <!-- integer, ids must be unique and flush -->
		<name>body_side_pressed</name> <!-- simply for debug purposes -->
		<frames>1</frames> <!-- must be an integer, 1 or higher -->
		<y_origin>0</y_origin> <!-- the top of an image is 0 -->
		<width>0</width>
		<height>0</height>
		<fps>0</fps> <!-- must be non-negative; 0 will turn into 1.0d so be sure that there is only one frame if you put 0 -->
	  </sprite>
	</mSpriteList>
    <mTagList></mTagList>
  </spritesheet>
  
</core_data>